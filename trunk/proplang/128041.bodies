class test_source
!!!128169.cpp!!!	main(in argc : int, in argv : char) : int
int counter; // <- The number of missing flags

// Test SRC_FLAG
PROPLANG_TEST_DEFINE(SRC_FLAG, counter);
PROPLANG_TEST_DEFINE(EXE_FLAG, counter);
PROPLANG_TEST_DEFINE(PROJ_FLAG, counter);
PROPLANG_TEST_DEFINE(LIB_LIB_HPP_, counter);
PROPLANG_TEST_DEFINE(EXE_EXE_HPP_, counter);

return (counter ? 1 : 0);
