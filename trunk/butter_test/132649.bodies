class verb_remove
!!!145961.python!!!	make(in elemname : unistr, in elemattr : dict) : object_interface
assert elemname == verb.element_name
assert elemattr.has_key (verb.type_attr)
if elemattr[verb.type_attr] == verb_remove.type_value:
  return verb_remove()
else:
  return None
